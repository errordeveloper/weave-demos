web:
  build: .
  command: python hello.py
  ports:
     - "5000:5000"
  volumes:
     - .:/code
  hostname: hello.weave.local
  environment:
     WEAVE_CIDR: ""
redis:
  image: redis
  hostname: redis.weave.local
  environment:
     WEAVE_CIDR: ""
